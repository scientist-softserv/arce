<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @collection.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @collection.content %>
</p>

<p>
  <strong>Video embed link:</strong>
  <%= @collection.video_embed_link %>
</p>

<p>
  <strong>Arts and culture embed:</strong>
  <%= @collection.arts_and_culture_embed %>
</p>

<% if current_user && !current_user.guest? %>
<%= link_to 'Edit', edit_collection_path(@collection) %> |
<%= link_to 'Delete Collection', @collection, method: :delete, data: { confirm: 'Are you sure?' } %> |
<% end %>
<%= link_to 'Back', root_path %>
