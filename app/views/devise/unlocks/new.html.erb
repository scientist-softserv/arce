<div class="container">
  <div class="u-heading-v2-5--bottom g-brd-primary "> 
    <h2 class="u-heading-v2__title text-uppercase g-font-weight-300 mb-0 g-mt-40">Resend unlock instructions</h2>
  </div>
  <div class="g-mt-40 g-mb-40">
    <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
      <%= devise_error_messages! %>

      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true %>
      </div>

      <div class="actions g-mt-40 g-mb-40">
        <%= f.submit "Resend unlock instructions" %>
      </div>
    <% end %>
  </div>
  <%= render "devise/shared/links" %>
</div>