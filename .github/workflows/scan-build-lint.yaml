name: "Build Lint"
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      debug_enabled:
        type: boolean
        description: 'Run the build with tmate debugging enabled (https://github.com/marketplace/actions/debugging-with-tmate)'
        required: false
        default: false

jobs:
  scan:
    uses: scientist-softserv/actions/.github/workflows/gitguardian.yaml@update-to-new-actions-library

jobs:
  build:
    uses: scientist-softserv/actions/.github/workflows/build.yaml@update-to-new-actions-library
    with:
      worker: true

  lint:
    needs: build
    uses: scientist-softserv/actions/.github/workflows/lint.yaml@v0.0.1
    with:
      worker: true
      # tag: deca90d9
