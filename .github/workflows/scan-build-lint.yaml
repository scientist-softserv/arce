name: "Scan Build Lint"
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      debug_enabled:
        type: boolean
        description: 'Run the build with tmate debugging enabled (https://github.com/marketplace/actions/debugging-with-tmate)'
        required: false
        default: false

jobs:
  scan:
    uses: scientist-softserv/actions/.github/workflows/gitguardian.yaml@self-hosted-runner

  build:
    uses: scientist-softserv/actions/.github/workflows/build.yaml@self-hosted-runner
    with:
      worker: true

  lint:
    needs: build
    uses: scientist-softserv/actions/.github/workflows/lint.yaml@self-hosted-runner
    with:
      worker: true
      # tag: deca90d9
